startCommand:
  type: stdio
  configSchema:
    type: object
    required: ["testApiKey", "testEndpoint"]
    properties:
      testApiKey:
        type: string
        description: "API key for test service authentication"
      testEndpoint:
        type: string
        description: "API endpoint URL for the test service"
